eval-hub-sdk[adapter]==0.4.0
# IBM CLEAR: now available on PyPI with agentic support included.
# 2.x nests per-agent results under reasoning_eval / tools_eval — see main.py extraction.
clear-eval==2.0.2

pandas>=2.0.0
pydantic>=2.0.0

# Required for fetching full traces (with spans) from MLflow when
# parameters.mlflow_traces_experiment_name is set.
mlflow-skinny>=2.0.0
