# RAGAS framework adapter requirements

# RAGAS evaluation framework
ragas>=0.4.0

# LLM/embeddings wrappers
openai>=1.0.0
langchain-core>=0.3.0

# EvalHub SDK (framework adapter with adapter extra only)
eval-hub-sdk[adapter]==0.1.5
